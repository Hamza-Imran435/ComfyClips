# Base URL of the standalone ComfyClips server (the server/ directory deployed
# on its own, e.g. as a separate Vercel project). Leave unset for local dev —
# Vite proxies /api to http://localhost:8787 instead (see vite.config.js).
API_URL=https://comfy-clips-nm8v.vercel.app
